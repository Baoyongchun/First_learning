package ${package.Service};

import ${package.Entity}.${entity};
import ${package.Mapper}.${table.mapperName};
import com.inspur.pub.web.AbstractService;
import org.slf4j.Logger;
import org.slf4j.LoggerFactory;
import org.springframework.stereotype.Service;

/**
 * $!{table.comment} 服务类
 *
 * @author ${author}
 * @since ${date}
 */
@Service
public class ${table.serviceName} extends AbstractService<${table.mapperName}, ${entity}, String> {
    private static final Logger logger = LoggerFactory.getLogger(${table.serviceName}.class);
}
